# SocialDebias 新一轮论文实验执行步骤

版本：paper-v2（基于论文—代码核对后的实现）  
固定随机种子：`42 / 2024 / 3407`  
适用目标：重新生成论文实验结果，再据实修改论文第 4、5、6 章及摘要。

## 从零开始完整执行入口

下面按“步骤 1 → 步骤 12”顺序执行。每一步都给出实际脚本命令；不要跳过当前步骤的
验收命令，也不要把不同 `EXP_ID` 的结果放到一起。

### 1. 初始化本轮实验环境

第一次在服务器上部署项目时执行：

```bash
cd /autodl-fs/data/socialdebias       # 改成服务器上的实际项目路径

# 仅在 socialvenv 不存在时创建
test -f socialvenv/bin/activate || python3 -m venv socialvenv
source socialvenv/bin/activate

python -m pip install --upgrade pip
python -m pip install -r requirements.txt
python -m spacy download en_core_web_sm
```

然后使用初始化脚本建立本轮独立结果目录并导出所有环境变量。`EXP_ID` 一旦开始训练就
不能更换；下面使用的日期 ID 可以替换一次，但后续每次登录必须继续使用同一个：

```bash
cd /autodl-fs/data/socialdebias
source scripts/init_paper_v2_env.sh paper_v2_20260719
```

这里必须使用 `source`，不能写成 `bash scripts/init_paper_v2_env.sh`，否则脚本结束后
`RUN_ROOT`、`MODEL_DIR` 等变量不会保留在当前终端。
脚本优先识别 `socialvenv`、`.venv`、`venv`，也支持已经激活的 virtualenv/conda。若环境
在其他位置，先执行 `export SOCIALDEBIAS_VENV_DIR=/实际/虚拟环境路径`。没有项目虚拟环境
但当前 `python` 可用时，脚本只会警告并恢复批次路径；继续训练前仍应建立固定环境。

初始化完成后应看到：

```text
[paper-v2] environment ready
EXP_ID=paper_v2_20260719
RUN_ROOT=/autodl-fs/data/socialdebias/results/paper_v2_20260719
```

立刻验证当前终端：

```bash
test -n "${EXP_ID}" \
  && test -d "${MODEL_DIR}" \
  && test -d "${LOG_DIR}" \
  && test -d "${RUN_ROOT}/manifests" \
  && echo "步骤 1 完成：本轮实验环境初始化成功"
```

SSH 断开或服务器重启后，不需要重新安装依赖。运行短的人工命令时可恢复同一批次；上文
列出的后台 runner 自带固定路径，不要求执行这一步：

```bash
cd /autodl-fs/data/socialdebias
source scripts/init_paper_v2_env.sh paper_v2_20260719
```

### 2～12. 后续完整执行顺序

| 步骤 | 执行内容 | 本文命令位置 | 完成标志 |
|---:|---|---|---|
| 2 | 代码、环境、NRC 和原始数据盘点 | P0（第 3 节） | 单测通过、NRC/原始数据齐全 |
| 3 | 三级过滤、NLI `p_entail` 和严格门禁 | P1（第 4 节）及 3.4 | 两个 ready 均为 True |
| 4 | BiLSTM/BERT/DeepSeek/ENDEF 基线 | P2（第 5 节） | 基线 history/JSON/CSV 齐全 |
| 5 | `surface_all` 主模型 | P3（第 6 节） | 9 个 checkpoint + history |
| 6 | SheepDog A/B/C/D 主评估 | P4（第 7 节） | `main_summary.csv` |
| 7 | SocialDebias-Adv | P5（第 8 节） | 24 个 filtered pkl + 三方 CSV |
| 8 | IG 解释一致性 | P6（第 9 节） | 3 个 JSON + 汇总 CSV |
| 9 | 六组件消融和 8/17 维对照 | P7/P8（第 10、11 节） | 42+3 组训练/评估齐全 |
| 10 | NLI 机制实验 | P9（第 12 节） | 45 个 history + 汇总 CSV |
| 11 | Adaptive λ 与人工评估 | P10/P11（第 13、14 节） | 18 组训练、12 组评估、盲标结果 |
| 12 | 最终验收并修改论文 | P12（第 15 节） | 全部机器可读结果冻结 |

如果此前只看到旧版 `training_ready=True`，先执行 4.4 的新版双门禁；确认
`paper_v2_data_ready=True` 和 `training_ready=True` 后，步骤 2、3 才算完成，可以直接从
第 5 节 P2 的 BERT 9 次训练开始。若是全新服务器，则从本页步骤 1 顺序执行直到 P12。

### 长时间任务统一后台执行（正式入口）

P1～P10 中耗时较长的机器实验统一使用下表脚本。脚本内部固定写入
`/autodl-fs/data/socialdebias/results/paper_v2_20260719`，不读取 `RUN_ROOT`、`MODEL_DIR`、
`LOG_DIR`，所以 SSH 断开或重新登录不会改变结果目录。全部脚本支持断点续跑：完整输出
自动跳过，缺失或过期的训练/评估自动补齐。

不要同时启动两个 GPU 训练阶段。应当按 P1 → P2 → … → P10 串行执行，并在当前阶段显示
`SUCCEEDED` 和对应的 `*_READY=True` 后再启动下一阶段。

| 阶段 | 后台任务名 | 恢复脚本 | 完成标志 |
|---|---|---|---|
| P1 | `p1_data` | `scripts/run_p1_data_prepare_resume.sh` | `P1_DATA_READY=True` |
| P2 核心基线 | `p2_core` | `scripts/run_p2_core_baselines_resume.sh` | `P2_CORE_BASELINES_READY=True` |
| P2 DeepSeek | `p2_deepseek` | `scripts/run_p2_deepseek_resume.sh` | `P2_DEEPSEEK_READY=True` |
| P2 ENDEF | `p2_endef` | `scripts/run_p2_endef_resume.sh` | `P2_ENDEF_READY=True` |
| P3 | `p3_surface_all` | `scripts/run_p3_surface_all_resume.sh` | `P3_SURFACE_ALL_READY=True` |
| P4 | `p4_surface_adv` | `scripts/run_p4_surface_adv_resume.sh` | `P4_SURFACE_ADV_READY=True` |
| P5 | `p5_socialdebias_adv` | `scripts/run_p5_socialdebias_adv_resume.sh` | `P5_SOCIALDEBIAS_ADV_READY=True` |
| P6 | `p6_explanation` | `scripts/run_p6_explanation_resume.sh` | `P6_EXPLANATION_READY=True` |
| P7 | `p7_ablation` | `scripts/run_p7_ablation_resume.sh` | `P7_ABLATION_READY=True` |
| P8 | `p8_surface17` | `scripts/run_p8_surface17_resume.sh` | `P8_SURFACE17_READY=True` |
| P9 | `p9_nli` | `scripts/run_p9_nli_resume.sh` | `P9_NLI_READY=True` |
| P10 | `p10_adaptive` | `scripts/run_p10_adaptive_resume.sh` | `P10_ADAPTIVE_READY=True` |

任一阶段的通用启动、查看状态和跟踪日志格式如下（以 P9 为例）：

```bash
cd /autodl-fs/data/socialdebias
bash scripts/paper_background_job.sh start p9_nli scripts/run_p9_nli_resume.sh
bash scripts/paper_background_job.sh status p9_nli
bash scripts/paper_background_job.sh log p9_nli
```

日志跟踪时按 `Ctrl-C` 只会退出 `tail -f`，不会停止后台任务。再次执行同一条 `start`
不会启动重复进程；任务结束后 `status` 明确输出 `SUCCEEDED exit=0` 或 `FAILED exit=非零值`，
并显示最后 20 行日志。失败后修复原因，再执行同一条 `start` 即可续跑。
若 P7/P8 是按旧版文档启动且仍在运行，控制器会识别旧 PID 并显示 `RUNNING/LEGACY`；此时
不要停止或重复启动，等原任务完成后继续下一阶段。

P2 DeepSeek 与 P5 需要 API 密钥。密钥只在启动时继承，不用于结果路径：

```bash
export DEEPSEEK_API_KEY='sk-xxxx'
bash scripts/paper_background_job.sh start p2_deepseek scripts/run_p2_deepseek_resume.sh

export DASHSCOPE_API_KEY='sk-xxxx'
export DEEPSEEK_API_KEY='sk-xxxx'
bash scripts/paper_background_job.sh start p5_socialdebias_adv scripts/run_p5_socialdebias_adv_resume.sh
```

下文不再重复展示训练或评估循环。每阶段只给出 runner 启动、状态、日志和验收命令；
具体训练参数以对应 `scripts/run_p*_resume.sh` 为唯一可执行定义。

## 1. 本轮实验的硬性原则

1. 旧 checkpoint 和旧表格数字全部视为 legacy，不与新结果混合求均值。
2. 所有神经网络主结果必须跑满三个随机种子；均值和标准差使用总体标准差
   （`numpy.std`，`ddof=0`）。
3. checkpoint 只按验证集 Macro-F1 选择，测试集只在选定 checkpoint 后报告，不能用
   test F1 选择配置。
4. 英文主干使用 `bert-base-uncased`，中文主干使用 `bert-base-chinese`；BERT 基线为
   全参数微调。
5. 统一训练参数：AdamW、`lr=2e-5`、`weight_decay=0.01`、
   `label_smoothing=0.1`、`max_length=512`、`epochs=3`；PolitiFact batch=4，
   GossipCop/Weibo21 batch=16。
6. SocialDebias 统一结构为 `768→384→128→2`，不得把旧 `768→384→2` checkpoint
   混入新实验。
7. 正式主模型在三个数据集上统一使用 `surface_all`，不再按数据集挑选不同“最优”
   配置。其组成固定为：GRL + Consist + 8 维 NRC + InfoNCE + NLI 双软标签 +
   Adaptive λ。
8. `surface`、`surface_adaptive`、17 维特征以及去组件版本只用于消融和机制分析。
9. 自适应 λ 是否触发必须读取本轮同 seed BERT 的 `best_val_f1` 和实际训练集规模，
   禁止在论文中预先写死 PolitiFact“不触发”。
10. 结果方向与旧论文不一致时，以新结果为准，修改论点，不为复现旧数字而调参。

## 2. 实验阶段与论文对应关系

| 阶段 | 内容 | 论文结果 |
|---|---|---|
| P0 | 环境、版本、资产和代码自检 | §5.2、附录 A |
| P1 | 三级过滤与 NLI `p_entail` 重建 | §4.3、§4.4、表 4-3 |
| P2 | BiLSTM、BERT、ENDEF、DeepSeek 基线 | §5.2.2、表 5-3～5-5 |
| P3 | 三数据集统一 `surface_all` 主模型 | 表 5-3～5-5 |
| P4 | SheepDog A/B/C/D 对抗评估 | 表 5-3、5-4 |
| P5 | SocialDebias-Adv 生成、过滤、三方评估 | 表 4-4、表 5-7 |
| P6 | IG 解释一致性 | 表 5-8、图 5.1～5.3 |
| P7 | 六组件消融 | 表 5-10 |
| P8 | 8 维与 17 维表层特征对照 | 表 5-11 |
| P9 | NLI 双软标签与 floor 敏感性 | 表 4-3、表 5-12、图 5.4 |
| P10 | 自适应 λ 跨数据集验证 | 表 5-13 |
| P11 | 人工评估 | §5.4.4 |
| P12 | 汇总、验收和论文回填 | 摘要、第 4～6 章 |

SheepDog 行继续引用原论文公开值，不在本地重新训练。所有本地方法与引用值必须在
表注中明确区分。

## 3. P0：建立独立实验批次

本节对应开头的“步骤 1”。如果已经执行过初始化脚本，不要再建立第二个批次；只恢复同一
`EXP_ID` 并确认 manifest：

```bash
cd /autodl-fs/data/socialdebias
source scripts/init_paper_v2_env.sh paper_v2_20260719

test -f "${RUN_ROOT}/manifests/git_commit.txt" \
  && test -f "${RUN_ROOT}/manifests/git_status.txt" \
  && test -f "${RUN_ROOT}/manifests/python_version.txt" \
  && test -f "${RUN_ROOT}/manifests/pip_freeze.txt" \
  && echo "P0 批次与 manifest 已就绪"
```

如果 `git status --short` 非空，应把 diff 一并保存，并在论文复现记录中说明使用的工作树
版本。不要在同一目录中把本轮 JSON 与旧 JSON 混放。

### 3.1 环境安装与静态自检

```bash
python -m pip install -r requirements.txt
python -m spacy download en_core_web_sm

python -m compileall -q modeling scripts utils tests \
  evaluate_endef_adversarial.py prepare_endef_data.py
python -m unittest discover -s tests -v
bash -n run_bert_baseline.sh run_llm_baseline.sh \
  run_socialdebias_adv.sh scripts/run_human_eval_sample.sh \
  scripts/run_human_eval_analysis.sh
```

验收条件：`unittest` 输出的全部测试均为 `OK`，compileall 和 `bash -n` 返回码均为 0。

### 3.2 资产门禁

```bash
test -f "${NRC_EN}" || echo "MISSING NRC_EN=${NRC_EN}"
test -f "${NRC_ZH}" || echo "MISSING NRC_ZH=${NRC_ZH}"
test -f data/sheepdog/news_articles/politifact_train.pkl || echo MISSING
test -f data/sheepdog/news_articles/gossipcop_train.pkl || echo MISSING
test -f data/weibo21_repo/data/train.pkl || echo MISSING
test -f data/qwen_adv/politifact_train_adv.pkl || echo MISSING
test -f data/qwen_adv/gossipcop_train_adv.pkl || echo MISSING
test -f data/qwen_adv/weibo21_train_adv_deepseek.pkl || echo MISSING
```

当前工作区路径检查显示：英文/中文 NRC 词典、三个新版 `p_entail` 文件和
SocialDebias-Adv 目录尚未提供；这里仅检查文件是否存在，没有核验数据内容。缺少 NRC
时不得用 `legacy_seed_v0` 跑正式论文实验。

使用统一审计脚本获取不展示新闻正文的结构化报告：

```bash
python scripts/audit_experiment_data.py \
  --json_output "${RUN_ROOT}/manifests/data_audit.json"
```

这是 P0 的首次盘点，不是最终验收。此时看到三个
`*_filtered_paper_v2.pkl`、三个 `*_p_entail_paper_v2.pkl` 和两个 NRC 文件显示
`MISSING/INVALID` 属于正常现象：前六个将在 P1 生成，两个 NRC 文件按 3.3 节准备。
P0 不要传 `--strict_training`。

### 3.3 NRC 官方包的本地准备

NRC EmoLex 研究版需由使用者在[官方许可页面](https://www.saifmohammad.com/WebPages/AccessResource.htm)
选择 Non-Commercial Research Use 后下载。官方条款要求引用并禁止重新分发，因此词典只保留
在本机 `data/lexicons/`，不要提交到 Git。官方资源包含 v0.92 英文词表及包括中文在内的
多语言自动翻译。

下载并解压后运行：

```bash
python scripts/prepare_nrc_emolex.py \
  --english_source /path/to/NRC-Emotion-Lexicon-Wordlevel-v0.92.txt \
  --translations /path/to/NRC-Emotion-Lexicon-v0.92-In105Languages.xlsx

python scripts/audit_experiment_data.py
```

转换器会生成 `${NRC_EN}`、`${NRC_ZH}`，并检查八类情绪是否齐全。如果官方翻译文件名
不同，以实际解压文件为准；`--translations` 支持 xlsx/csv/tsv。

### 3.4 两个 ready 均为 True 后如何继续

P1 后台脚本已自动执行严格门禁。若要单独复核，使用固定目录命令：

```bash
cd /autodl-fs/data/socialdebias
mkdir -p /autodl-fs/data/socialdebias/results/paper_v2_20260719/manifests \
         /autodl-fs/data/socialdebias/results/paper_v2_20260719/logs
python scripts/audit_experiment_data.py \
  --strict_paper_v2 \
  --strict_training \
  --json_output /autodl-fs/data/socialdebias/results/paper_v2_20260719/manifests/data_audit_final.json \
  2>&1 | tee /autodl-fs/data/socialdebias/results/paper_v2_20260719/logs/data_audit_final.log
```

终端最后同时显示 `paper_v2_data_ready=True` 和 `training_ready=True`，表示三份过滤集、
三份 `p_entail` 和两份 NRC 词典都已通过门禁。此后不要再重跑 P1，也不要改写这 8 个
输入文件；直接从 P2 开始。这只表示“实验训练数据门禁通过”，不表示 BERT checkpoint
已存在。

远程机器重新登录后，shell 中的变量会丢失。只有执行下文保留的手工审计命令时才需要
恢复；正式长任务直接使用固定目录后台入口：

```bash
cd /autodl-fs/data/socialdebias       # 按服务器实际路径修改
source scripts/init_paper_v2_env.sh paper_v2_20260719

: "${EXP_ID:?EXP_ID 未恢复}"
: "${RUN_ROOT:?RUN_ROOT 未恢复}"
: "${MODEL_DIR:?MODEL_DIR 未恢复}"
: "${LOG_DIR:?LOG_DIR 未恢复}"
printf 'EXP_ID=%s\nRUN_ROOT=%s\nMODEL_DIR=%s\n' \
  "${EXP_ID}" "${RUN_ROOT}" "${MODEL_DIR}"
```

使用统一后台脚本按以下顺序串行执行，避免显存争抢和结果依赖缺失：

```text
P2 BERT 9 次 → BERT 对抗评估 6 次 → BiLSTM 6 次 → DeepSeek/ENDEF
→ P3 主模型 9 次 → P4 主模型对抗评估 6 次
→ P5 SocialDebias-Adv → P6 IG
→ P7 消融 42 次 → P8 17 维 3 次
→ P9 NLI 45 次 → P10 Adaptive 18 次 → P11 人工评估 → P12 回填论文
```

如果只有一张 GPU，同一时刻只启动一个阶段；不需要 `tmux`。启动、状态与日志查看均使用
开头“长时间任务统一后台执行”的 `paper_background_job.sh`。

每个阶段结束先检查文件数量和汇总 CSV，再开始下一阶段。磁盘不足时先查看
`du -sh "${RUN_ROOT}"` 和 `df -h`，不要删除尚未汇总或尚未评估的 checkpoint。

## 4. P1：重建三级过滤训练对抗集

P1 负责生成三份过滤集和三份带 `p_entail` 的训练集。正式执行只使用恢复脚本，不再手工
运行英文过滤、中文过滤或 NLI 脚本。

### 4.1 启动与查看

```bash
cd /autodl-fs/data/socialdebias
bash scripts/paper_background_job.sh start p1_data scripts/run_p1_data_prepare_resume.sh
bash scripts/paper_background_job.sh status p1_data
bash scripts/paper_background_job.sh log p1_data
```

日志出现 `P1_DATA_READY=True` 且状态为 `SUCCEEDED exit=0` 后验收：

```bash
cd /autodl-fs/data/socialdebias
test -s data/qwen_adv/politifact_train_adv_filtered_paper_v2.pkl \
  && test -s data/qwen_adv/gossipcop_train_adv_filtered_paper_v2.pkl \
  && test -s data/qwen_adv/weibo21_train_adv_filtered_paper_v2.pkl \
  && test -s data/qwen_adv/politifact_p_entail_paper_v2.pkl \
  && test -s data/qwen_adv/gossipcop_p_entail_paper_v2.pkl \
  && test -s data/qwen_adv/weibo21_p_entail_paper_v2.pkl \
  && test -s results/paper_v2_20260719/data_preparation/after.json \
  && echo "P1 六份数据和审计报告齐全"
```

`after.json` 必须同时对应 `paper_v2_data_ready=True` 和 `training_ready=True`。三级阈值、
NLI 模型参数和输出格式以 `scripts/run_p1_data_prepare_resume.sh` 及其调用的
`prepare_paper_v2_data.sh` 为唯一依据。失败时修复日志中的具体问题，再执行同一条 `start`；
不要绕过 runner 单独覆盖某个正式输出。

## 5. P2：重新训练基线

P2 拆成核心基线、DeepSeek 和 ENDEF 三个串行后台任务。先完成 `p2_core`，再执行另外两项。

### 5.1 BiLSTM、BERT 与 BERT 对抗评估

```bash
cd /autodl-fs/data/socialdebias
bash scripts/paper_background_job.sh start p2_core scripts/run_p2_core_baselines_resume.sh
bash scripts/paper_background_job.sh status p2_core
bash scripts/paper_background_job.sh log p2_core
```

看到 `P2_CORE_BASELINES_READY=True` 后检查：

```bash
cd /autodl-fs/data/socialdebias
test "$(find results/paper_v2_20260719/lstm -maxdepth 1 -name 'lstm_*_result.json' | wc -l)" -eq 6 \
  && test "$(find results/paper_v2_20260719/models -maxdepth 1 -name '*_bert_baseline_history.json' | wc -l)" -eq 9 \
  && test "$(find results/paper_v2_20260719/bert_adv -maxdepth 1 -name 'bert_adv_*.json' | wc -l)" -eq 6 \
  && test -s results/paper_v2_20260719/lstm_summary.csv \
  && echo "P2 核心基线完成"
```

正式 BiLSTM 不使用 GloVe；BERT 为三数据集、三 seed 全参数微调。所有训练参数以
`scripts/run_p2_core_baselines_resume.sh` 为唯一执行定义。

### 5.2 DeepSeek 零样本

```bash
cd /autodl-fs/data/socialdebias
export DEEPSEEK_API_KEY='sk-xxxx'
bash scripts/paper_background_job.sh start p2_deepseek scripts/run_p2_deepseek_resume.sh
bash scripts/paper_background_job.sh status p2_deepseek
bash scripts/paper_background_job.sh log p2_deepseek
```

看到 `P2_DEEPSEEK_READY=True` 后检查：

```bash
cd /autodl-fs/data/socialdebias
test "$(find results/paper_v2_20260719/llm_baseline -maxdepth 1 -name '*.json' | wc -l)" -eq 11 \
  && test -s results/paper_v2_20260719/llm_baseline/summary.csv \
  && echo "DeepSeek 11/11 完成"
```

模型 ID 默认固定为 `deepseek-v4-flash`。缓存会核对模型 ID，断线后重新启动同一任务即可
续跑，不能把其他模型的 JSON 改名后复用。

### 5.3 ENDEF

ENDEF 使用独立虚拟环境。仅在 `/autodl-fs/data/ENDEF-SIGIR2022/endefvenv` 不存在或损坏时，
先执行一次环境安装：

```bash
cd /autodl-fs/data/socialdebias
bash scripts/setup_endef_env.sh /autodl-fs/data/ENDEF-SIGIR2022
```

随后只通过后台恢复脚本转换数据、训练 18 个任务、及时备份 checkpoint 并完成英文对抗
评估：

```bash
cd /autodl-fs/data/socialdebias
bash scripts/paper_background_job.sh start p2_endef scripts/run_p2_endef_resume.sh
bash scripts/paper_background_job.sh status p2_endef
bash scripts/paper_background_job.sh log p2_endef
```

看到 `P2_ENDEF_READY=True` 后检查：

```bash
test "$(find /autodl-fs/data/ENDEF-SIGIR2022/ENDEF_en/backup_ckpt -maxdepth 1 -name '*.pkl' | wc -l)" -eq 12 \
  && test "$(find /autodl-fs/data/ENDEF-SIGIR2022/ENDEF_ch/backup_ckpt -maxdepth 1 -name '*.pkl' | wc -l)" -eq 6 \
  && test "$(find /autodl-fs/data/socialdebias/results/paper_v2_20260719/endef_adv -maxdepth 1 -name 'endef_adv_*.json' | wc -l)" -eq 12 \
  && echo "ENDEF 训练与英文评估完成"
```

runner 会自动安装中文标签兼容补丁、创建 `logs/json` 等运行目录并跳过已有非空备份。
若环境报 NumPy/pandas ABI 错误，重新执行 `setup_endef_env.sh` 后续跑；若报 `KeyError: 0`，
确认服务器已同步新版 `patch_endef_ch_dataloader.py`。不要手工复制共享 checkpoint 到猜测的
数据集或 seed 文件名。

## 6. P3：统一主模型 `surface_all`

三个数据集统一使用 GRL + Consist + 8 维 NRC + InfoNCE + NLI 双软标签 + Adaptive λ。
正式执行只使用：

```bash
cd /autodl-fs/data/socialdebias
bash scripts/paper_background_job.sh start p3_surface_all scripts/run_p3_surface_all_resume.sh
bash scripts/paper_background_job.sh status p3_surface_all
bash scripts/paper_background_job.sh log p3_surface_all
```

看到 `P3_SURFACE_ALL_READY=True` 后检查：

```bash
cd /autodl-fs/data/socialdebias
test "$(find results/paper_v2_20260719/models -maxdepth 1 -name '*_surface_all.pt' | wc -l)" -eq 9 \
  && test "$(find results/paper_v2_20260719/models -maxdepth 1 -name '*_surface_all_history.json' | wc -l)" -eq 9 \
  && test -s results/paper_v2_20260719/surface_all_clean_summary.csv \
  && echo "P3 surface_all 9/9 完成"
```

每份 history 必须记录 `best_val_f1`、`best_test`、`bias_acc`、六项 loss、
`adaptive_triggered`、触发理由和实际 λ。不得使用第四个 seed 替换失败 seed；重复启动 runner
只会补齐缺失项。

## 7. P4：主模型 SheepDog A/B/C/D 评估与汇总

```bash
cd /autodl-fs/data/socialdebias
bash scripts/paper_background_job.sh start p4_surface_adv scripts/run_p4_surface_adv_resume.sh
bash scripts/paper_background_job.sh status p4_surface_adv
bash scripts/paper_background_job.sh log p4_surface_adv
```

看到 `P4_SURFACE_ADV_READY=True` 后检查：

```bash
cd /autodl-fs/data/socialdebias
test "$(find results/paper_v2_20260719/surface_adv -maxdepth 1 -name 'surface_adv_*_surface_all.json' | wc -l)" -eq 6 \
  && test -s results/paper_v2_20260719/main_summary.csv \
  && echo "P4 主模型英文对抗评估 6/6 完成"
```

`main_summary.csv` 中每个模型/数据集/测试分组必须 `n=3`；所有 JSON 必须包含
`avg_adv_f1`、`f1_drop`、`avg_asr` 以及 A/B/C/D paired ASR。

## 8. P5：SocialDebias-Adv

P5 统一完成 24 组 API 生成、三级过滤以及 BERT/DeepSeek/SocialDebias 三方评估。

```bash
cd /autodl-fs/data/socialdebias
export DASHSCOPE_API_KEY='sk-xxxx'
export DEEPSEEK_API_KEY='sk-xxxx'
bash scripts/paper_background_job.sh start p5_socialdebias_adv scripts/run_p5_socialdebias_adv_resume.sh
bash scripts/paper_background_job.sh status p5_socialdebias_adv
bash scripts/paper_background_job.sh log p5_socialdebias_adv
```

看到 `P5_SOCIALDEBIAS_ADV_READY=True` 后检查：

```bash
cd /autodl-fs/data/socialdebias
test "$(find data/socialdebias_adv -maxdepth 1 -name '*_test_adv_*.pkl' | wc -l)" -eq 24 \
  && test "$(find data/socialdebias_adv/filtered -maxdepth 1 -name '*.pkl' | wc -l)" -eq 24 \
  && test -s data/socialdebias_adv/filtered/filter_report.csv \
  && test -s results/paper_v2_20260719/socialdebias_adv_eval.csv \
  && echo "P5 生成、过滤和三方评估完成"
```

默认模型固定为 `qwen3.6-plus-2026-04-02` 与 `deepseek-v4-flash`。生成器逐任务保存并校验
模型 ID；断线后执行同一条 `start`。论文表 4-4 的生成量、保留量和保留率必须由本轮
`filter_report.csv` 重算。

## 9. P6：IG 解释一致性

```bash
cd /autodl-fs/data/socialdebias
bash scripts/paper_background_job.sh start p6_explanation scripts/run_p6_explanation_resume.sh
bash scripts/paper_background_job.sh status p6_explanation
bash scripts/paper_background_job.sh log p6_explanation
```

看到 `P6_EXPLANATION_READY=True` 后检查：

```bash
cd /autodl-fs/data/socialdebias
test "$(find results/paper_v2_20260719/explanation -maxdepth 1 -name 'politifact_surface_all_seed*.json' | wc -l)" -eq 3 \
  && test -s results/paper_v2_20260719/explanation/explanation_3seed_summary.csv \
  && echo "P6 IG 三 seed 完成"
```

指标固定为零 embedding baseline、50 积分步、Top-10 交集/K、完整归因分布 Softmax 后 JS；
对照使用同 seed 的正式 BERT checkpoint。论文报告三 seed 汇总，不能把 seed 42 单次结果
称为稳定提升。

## 10. P7：六组件消融

完整模型加 6 个去组件变体，共 7 个配置；PolitiFact 和 GossipCop 各三 seed，共 42 次
训练和 42 次评估。配置定义如下：

| 后缀 | 相对 Full 的唯一变化 |
|---|---|
| `abl_full` | 全组件 |
| `abl_no_grl` | 去掉 GRL |
| `abl_no_infonce` | 去掉 InfoNCE，保留分类软标签 |
| `abl_no_consist` | 去掉 Consist |
| `abl_no_surface` | 去掉 NRC 表层特征 |
| `abl_no_advaug` | 去掉 paired 对抗增强及其 NLI 机制 |
| `abl_no_labelsmooth` | 去掉分类 label smoothing，保留加权 InfoNCE |

只使用以下后台入口，不再手工执行 42 次循环：

```bash
cd /autodl-fs/data/socialdebias
bash scripts/paper_background_job.sh start p7_ablation scripts/run_p7_ablation_resume.sh
bash scripts/paper_background_job.sh status p7_ablation
bash scripts/paper_background_job.sh log p7_ablation
```

若旧版 P7 已经在运行，控制器显示 `RUNNING/LEGACY`；不要停止或重复启动。日志查看时
`Ctrl-C` 只退出 `tail -f`。看到 `P7_ABLATION_READY=True` 后检查：

```bash
cd /autodl-fs/data/socialdebias
test "$(find results/paper_v2_20260719/models -maxdepth 1 -name '*_abl_*_history.json' | wc -l)" -eq 42 \
  && test "$(find results/paper_v2_20260719/ablation_adv -maxdepth 1 -name 'ablation_adv_*_abl_*.json' | wc -l)" -eq 42 \
  && test -s results/paper_v2_20260719/ablation_clean_summary.csv \
  && test -s results/paper_v2_20260719/ablation_adv/ablation_adv_summary.csv \
  && echo "P7 消融训练/评估 42/42 完成"
```

`abl_no_infonce` 的 history 必须记录
`paired_forward_version=skip_unused_orig_v1`。runner 会自动重跑旧实现生成的这 6 个任务，
其他有效任务跳过，并在 checkpoint 更新后刷新对应评估。评估遇到 list/float 标准化错误时，
同步新版 `evaluate_surface_adv.py` 后重新启动同一 runner，不要手工重跑训练。

## 11. P8：8 维与 17 维特征对照

固定 PolitiFact Full 配置和三个 seed；8 维复用 P7 的 `abl_full`，只新增三个 17 维任务。

```bash
cd /autodl-fs/data/socialdebias
bash scripts/paper_background_job.sh start p8_surface17 scripts/run_p8_surface17_resume.sh
bash scripts/paper_background_job.sh status p8_surface17
bash scripts/paper_background_job.sh log p8_surface17
```

看到 `P8_SURFACE17_READY=True` 后检查：

```bash
cd /autodl-fs/data/socialdebias
test "$(find results/paper_v2_20260719/models -maxdepth 1 -name '*_surface17_full_history.json' | wc -l)" -eq 3 \
  && test "$(find results/paper_v2_20260719/ablation_adv -maxdepth 1 -name '*_surface17_full_seed*.json' | wc -l)" -eq 3 \
  && test -s results/paper_v2_20260719/surface_8_vs_17_clean_summary.csv \
  && echo "P8 17 维训练/评估 3/3 完成"
```

若 17 维结果更好，必须据实修改论文“少即是多”的结论。

## 12. P9：NLI 机制实验

五种配置在三个数据集、三个 seed 上运行，共 45 次。统一保留 paired 数据并使用固定 λ：

| 配置 | InfoNCE 的 NLI 权重 | 分类软标签 | floor |
|---|---:|---:|---:|
| `nli_hard` | 否 | 否 | — |
| `nli_contrast_only` | 是 | 否 | 0.5 |
| `nli_cls_only` | 否 | 是 | 0.5 |
| `nli_dual_floor0` | 是 | 是 | 0.0 |
| `nli_soft14` | 是 | 是 | 0.5 |

只使用以下后台入口：

```bash
cd /autodl-fs/data/socialdebias
bash scripts/paper_background_job.sh start p9_nli scripts/run_p9_nli_resume.sh
bash scripts/paper_background_job.sh status p9_nli
bash scripts/paper_background_job.sh log p9_nli
```

看到 `P9_NLI_READY=True` 后检查：

```bash
cd /autodl-fs/data/socialdebias
test "$(find results/paper_v2_20260719/models -maxdepth 1 -name '*_nli_*_history.json' | wc -l)" -eq 45 \
  && test -s results/paper_v2_20260719/nli_mechanism_summary.csv \
  && test -s results/paper_v2_20260719/nli_mechanism_summary_details.csv \
  && echo "P9 NLI 机制实验 45/45 完成"
```

在 details CSV 中核对五个后缀的 `lambda_contrast`、`lambda_fact_soft` 和两个 floor。
`nli_cls_only` 使用已修复的 paired forward 实现。不得继续沿用旧的 36 次实验说法。

## 13. P10：Adaptive λ 跨数据集验证

`surface_fixed` 与 `surface_adaptive` 各在三个数据集、三个 seed 上训练，共 18 次；随后对
两个英文数据集完成 12 次 A/B/C/D 评估。两组都不启用 InfoNCE/NLI，唯一差异是 Adaptive λ。

```bash
cd /autodl-fs/data/socialdebias
bash scripts/paper_background_job.sh start p10_adaptive scripts/run_p10_adaptive_resume.sh
bash scripts/paper_background_job.sh status p10_adaptive
bash scripts/paper_background_job.sh log p10_adaptive
```

看到 `P10_ADAPTIVE_READY=True` 后检查：

```bash
cd /autodl-fs/data/socialdebias
test "$(find results/paper_v2_20260719/models -maxdepth 1 \
  \( -name '*_surface_fixed_history.json' -o -name '*_surface_adaptive_history.json' \) | wc -l)" -eq 18 \
  && test "$(find results/paper_v2_20260719/surface_adv -maxdepth 1 \
  \( -name '*_surface_fixed.json' -o -name '*_surface_adaptive.json' \) | wc -l)" -eq 12 \
  && test -s results/paper_v2_20260719/adaptive_clean_summary.csv \
  && test -s results/paper_v2_20260719/surface_fixed_adv_summary.csv \
  && test -s results/paper_v2_20260719/surface_adaptive_adv_summary.csv \
  && echo "P10 Adaptive 训练与评估完成"
```

从 `adaptive_clean_summary_details.csv` 核对训练规模、同 seed BERT 验证 F1、是否触发、
触发原因和缩放前后 λ。固定组必须是 `adaptive_enabled=False`；表 5-13 中是否触发完全以
本轮记录为准。

## 14. P11：人工评估

当前人工实验对应 PolitiFact 原文与 SheepDog `adv_C`，不是 P5 的 24 组
SocialDebias-Adv。本轮复用已经完成的 `appendix_c_human_eval_fixed.csv`：共 100 行、50 个
样本对，real/fake 各 25 对，每对均含 `original` 和 `adv_C`，关键词、判断和置信度完整。
文件中的 50 个 ID 已核对，与当前 PolitiFact 数据按 `n_samples=50, seed=42` 得到的样本
集合完全一致。
该文件是 GB18030 编码并使用中文列名，新版分析脚本会自动识别、验证并输出规范 UTF-8
副本。先把文件上传或同步到项目根目录，再检查：

```bash
cd /autodl-fs/data/socialdebias
test -s appendix_c_human_eval_fixed.csv \
  && echo "旧人工标注文件已就绪"
```

直接计算人工判别、原文/对抗变化、人工关键词一致性，并与 P6 seed 42 的 IG 做案例对齐：

```bash
cd /autodl-fs/data/socialdebias
mkdir -p results/paper_v2_20260719/human_eval/final \
         results/paper_v2_20260719/logs
python scripts/analyze_human_eval.py \
  --input appendix_c_human_eval_fixed.csv \
  --ig_json results/paper_v2_20260719/explanation/politifact_surface_all_seed42.json \
  --topk 10 --uncertain_score 0.5 \
  --output_dir results/paper_v2_20260719/human_eval/final \
  2>&1 | tee results/paper_v2_20260719/logs/human_eval_analysis.log
```

验收 `final/` 下的 `human_eval_input_manifest.json`、`human_eval_normalized_input.csv`、
`human_eval_metrics.json`、`human_eval_per_sample.csv` 和（成功匹配 IG 时）
`human_eval_model_alignment.csv`。manifest 保存原文件 SHA-256、识别编码、行数和样本对数。

这是一份既有的单人最终标注文件，不得将其复制成两份后计算一致性，也不运行
`analyze_annotator_agreement.py`。论文应明确标为单标注者或既有裁决结果，不报告 Cohen's
Kappa；如果无法证明当时隐藏了真实标签和文本类型，也不能新增“严格盲标”的表述。
模型—人工关键词对齐仅作为 seed 42 案例分析；若要声称跨 seed 稳定，必须对三个 P6 JSON
分别分析再汇总。

## 15. P12：结果验收与论文修改顺序

### 15.1 每组实验的最低验收条件

- 三 seed 结果数量必须为 3，不完整的组不能进论文主表。
- JSON/config 中的结构、损失权重、数据集、语言、词典版本和 API 模型 ID 与计划一致。
- 所有均值/std 从单 seed 原始 JSON 重新计算，禁止从终端截图手抄后再平均。
- paired ASR 分母是 clean 正确样本数；标签不一致的结果作废。
- 对抗 F1 为 A/B/C/D 四组 Macro-F1 的算术平均；F1 Drop=`Clean F1-Avg Adv F1`。
- API 解析失败率必须报告；不能把无法解析的输出静默删除。
- 不以“接近旧论文数值”为验收标准，只以流程、配置和产物完整性为标准。

### 15.2 结果汇总清单

最终应得到以下机器可读文件：

1. `${RUN_ROOT}/main_summary.csv`：BERT 与统一 Full 模型的英文 clean/adv/ASR。
2. `${RUN_ROOT}/lstm_summary.csv`、`surface_all_clean_summary.csv`：基线和主模型 clean 结果。
3. 三数据集 BERT、SocialDebias history JSON：clean Acc/F1/AUC、bias acc、adaptive 记录。
4. `${RUN_ROOT}/ablation_clean_summary.csv`、`${RUN_ROOT}/ablation_adv/ablation_adv_summary.csv`：
   六组件结果。
5. `${RUN_ROOT}/surface_8_vs_17_clean_summary.csv`：8/17 维对照。
6. `${RUN_ROOT}/nli_mechanism_summary.csv`：五组 NLI 机制结果。
7. `${RUN_ROOT}/adaptive_clean_summary.csv`、`surface_fixed_adv_summary.csv`、
   `surface_adaptive_adv_summary.csv`：自适应 λ 对照。
8. `${RUN_ROOT}/socialdebias_adv_eval.csv`：三方、四风格、双来源结果。
9. `${RUN_ROOT}/explanation/explanation_3seed_summary.csv`：三 seed IG 指标。
10. `${RUN_ROOT}/llm_baseline/summary.csv`：DeepSeek 零样本结果。
11. `${RUN_ROOT}/endef_adv/*.json`：ENDEF paired ASR。
12. 过滤报告、人工评估 JSON/CSV、完整日志和环境 manifest。

全部实验结束后，不要手工复制各 CSV。执行统一报告生成器，它会先验收 P1～P11 的文件
数量与必要汇总，再按照第 4、5、6 章和表 4-3、4-4、5-3～5-13 生成本轮结果、旧论文值、
差值、来源路径和论文修改建议：

```bash
cd /autodl-fs/data/socialdebias
python scripts/build_paper_results_report.py --require_complete
```

无论验收是否通过，都会先生成：

- `results/paper_v2_20260719/论文实验结果总验收与旧论文对照.md`：供人工核对和修改论文；
- `results/paper_v2_20260719/论文实验结果总验收与旧论文对照.json`：机器可读验收、主结果和来源。

终端显示 `Acceptance: PASS` 才表示完整验收通过。若显示 `FAIL`，命令退出码为 2；直接打开
Markdown 开头的验收表和末尾“尚未通过的验收项”，补齐对应产物后重新执行同一命令。
旧论文对照值固定保存在 `paper_reference/legacy_results_v1.json`，不得把旧值写死到新结果
CSV，也不得将旧 seed 与本轮 seed 合并。

### 15.3 修改论文的顺序

1. 先更新 §5.2 的实现配置、模型结构、随机种子、API 模型 ID 和指标定义。
2. 更新表 4-3、4-4 的过滤量与 NLI 配置；删除无法由日志支撑的比例描述。
3. 更新表 5-3～5-5 主结果，只使用统一 `surface_all`。
4. 更新表 5-7 三方评估，三个数据集都应包含 BERT、DeepSeek、SocialDebias。
5. 更新表 5-8 及解释一致性文字，使用修正后的 Top-K、Spearman、JS。
6. 更新表 5-10～5-13、图 5.1～5.4，并根据实际方向重写组件贡献和自适应机制结论。
7. 最后修改摘要、创新点、第 5 章小结、第 6 章总结；这些位置不能保留旧数字。
8. 全文搜索旧核心数字和模型名，避免正文、图注、摘要与附录互相矛盾。

建议全文搜索：

```bash
rg -n "0\.8703|0\.8771|0\.7549|0\.9112|0\.1446|0\.4707|0\.1750|\
2,234|57\.0%|DeepSeek-V4|768→384|Jaccard|surface_adaptive|surface_all" \
  /path/to/thesis/source
```

本轮结束后，先冻结 `${RUN_ROOT}`，再根据其中的 CSV/JSON 修改论文；不要边跑实验边修改
结论，以免不同阶段的数字混入同一版本。
